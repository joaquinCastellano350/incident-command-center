# Standardize application UI on shadcn/ui

All application UI will compose source-controlled shadcn/ui primitives discovered through the connected registry workflow. This trades bespoke component freedom for consistent behavior, accessibility, maintainability, and a restrained flat visual system; project-specific styling belongs in semantic tokens and domain compositions rather than replacement primitives, gradients, or ornamental effects.
