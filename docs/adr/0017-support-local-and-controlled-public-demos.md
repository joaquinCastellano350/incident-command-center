# Support local and controlled public demos

The project will support one-command local startup through Docker Compose and a public managed-container deployment backed by managed PostgreSQL. API and worker processes will run different commands from the same versioned image, while the public synthetic workspace uses a stable demo Operator, lightweight access control for mutations and model calls, rate limits, quotas, and resettable seeded data rather than full account or organization management.
