# Run demo scenarios through provider adapters

Seeded demo scenarios will submit realistic provider-shaped payloads through the same ingestion adapter interfaces as webhook endpoints rather than inserting normalized records directly. Deterministic scenario seeds will make demonstrations and tests repeatable while exercising normalization, deduplication, durable jobs, policy, and workflow behavior end to end.
