# Use an end-to-end TypeScript monorepo

The MVP will use TypeScript across a monorepo containing a Next.js operator dashboard, a Fastify API, an asynchronous worker, framework-independent domain behavior, versioned contracts, and shared testing utilities. TypeSafe, generative-model, storage, and simulated provider SDKs will remain behind adapters so the unified language does not collapse the module boundaries.
